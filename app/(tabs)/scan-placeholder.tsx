import React from "react";
import { View } from "react-native";

// Placeholder screen for the center scan button tab
// This screen is never shown — the tab press navigates to /scanner
export default function ScanPlaceholder() {
    return <View />;
}
